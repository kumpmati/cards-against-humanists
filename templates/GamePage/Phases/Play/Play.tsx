import React from "react";
import styles from "./style.module.css";

const Play = (game: any) => {
  console.log(game);
  return (
    <div className={styles.container}>
      <h1>coming soon...</h1>
    </div>
  );
};

export default Play;
